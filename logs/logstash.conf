input {
  beats {
    port => 5044
  }
}

filter {
  json {
    source => "message" # Assumes that Winston logs are formatted as JSON
  }
}

output {
  stdout { codec => rubydebug }
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "your-logs-%{+YYYY.MM.dd}"
  }
}